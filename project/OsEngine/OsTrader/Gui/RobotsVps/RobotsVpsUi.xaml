<Window x:Class="OsEngine.OsTrader.Gui.RobotsVps.RobotsVpsUi"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OsEngine.OsTrader.Gui.RobotsVps"
        mc:Ignorable="d"
        Title="Bot Station VPS" Height="750" Width="1100"
        Style="{StaticResource WindowStyleCanResize}"
        WindowStartupLocation="CenterScreen"
        MinWidth="700" MinHeight="450"
        Icon="/Images/OsLogo.ico">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*" MinHeight="200"/>
            <RowDefinition Height="150" MinHeight="100"/>
        </Grid.RowDefinitions>

        <!-- Строка подключения: адрес удалённого MCP API (обычно через SSH-туннель) и ключ -->
        <Grid Grid.Row="0" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Name="LabelUrl" Content="MCP URL" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" Name="TextBoxUrl" VerticalAlignment="Center" Height="26" Margin="0,0,10,0"
                     Text="http://localhost:6510/api/v2/mcp"/>

            <Label Grid.Column="2" Name="LabelApiKey" Content="API Key" VerticalAlignment="Center"/>
            <PasswordBox Grid.Column="3" Name="PasswordBoxApiKey" VerticalAlignment="Center" Height="26" Margin="0,0,10,0"/>

            <Button Grid.Column="4" Name="ButtonConnect" Content="Connect" Width="100" Height="28" Margin="0,0,5,0" Click="ButtonConnect_Click"/>
            <Button Grid.Column="5" Name="ButtonDisconnect" Content="Disconnect" Width="100" Height="28" Margin="0,0,10,0" Click="ButtonDisconnect_Click" IsEnabled="False"/>

            <Ellipse Grid.Column="6" Name="EllipseStatus" Width="14" Height="14" Fill="Gray" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <Label Grid.Column="6" Name="LabelStatus" Content="Disconnected" HorizontalAlignment="Left" Margin="20,0,0,0" VerticalAlignment="Center"/>
        </Grid>

        <!-- Серверы и боты удалённого терминала -->
        <TabControl Grid.Row="1" Name="TabControlPrime" BorderThickness="0" Margin="5,0,5,5">
            <TabItem Name="TabServers" Header="Servers" Width="140">
                <Grid Margin="0,5,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <DataGrid Name="ServersDataGrid"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              Style="{StaticResource DataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Type" Width="220" Binding="{Binding Type}"/>
                            <DataGridTextColumn Header="Number" Width="80" Binding="{Binding Number}"/>
                            <DataGridTextColumn Header="Status" Width="120" Binding="{Binding Status}"/>
                            <DataGridTemplateColumn Header="Connect" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Connect" Click="ServerConnectButton_Click" Tag="{Binding}" Padding="5,2"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Disconnect" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Disconnect" Click="ServerDisconnectButton_Click" Tag="{Binding}" Padding="5,2"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Name="TabBots" Header="Bots" Width="140">
                <Grid Margin="0,5,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DataGrid Grid.Row="0" Name="BotsDataGrid"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              SelectionChanged="BotsDataGrid_SelectionChanged"
                              Style="{StaticResource DataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Number" Width="80" Binding="{Binding Number}"/>
                            <DataGridTextColumn Header="Name" Width="220" Binding="{Binding Name}"/>
                            <DataGridTextColumn Header="Class" Width="220" Binding="{Binding ClassName}"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Сводка по выбранному боту: bot_journal_get_summary + get_statistics (опрос, без push-событий — их пока нет на сервере) -->
                    <Grid Grid.Row="1" Margin="0,5,0,0" Background="{DynamicResource PanelAltBrush}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Label Grid.Column="0" Content="P/L, abs" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        <Label Grid.Column="1" Name="LabelBotProfitAbs" Content="—" VerticalAlignment="Center"/>
                        <Label Grid.Column="2" Content="P/L, %" VerticalAlignment="Center"/>
                        <Label Grid.Column="3" Name="LabelBotProfitPercent" Content="—" VerticalAlignment="Center"/>
                        <Label Grid.Column="4" Content="Deals" VerticalAlignment="Center"/>
                        <Label Grid.Column="5" Name="LabelBotDeals" Content="—" VerticalAlignment="Center"/>
                        <Label Grid.Column="6" Content="Commission" VerticalAlignment="Center"/>
                        <Label Grid.Column="7" Name="LabelBotCommission" Content="—" VerticalAlignment="Center"/>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Лог: события SSE удалённого сервера (server_instance.*, terminal.*) + сообщения этого окна -->
        <Grid Grid.Row="2" Name="LogGrid" Margin="5" Background="{DynamicResource PanelAltBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Rectangle Height="29" Stroke="{DynamicResource PanelAltBorderBrush}" VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="5,3,1,0"/>
                <Label Content="Log" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,6,0,0" FontSize="12"/>
            </Grid>

            <DataGrid Grid.Row="1" Name="LogDataGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      Margin="5,5,1,1"
                      Style="{StaticResource DataGridStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Width="150" Binding="{Binding Time, StringFormat='{}{0:HH:mm:ss}'}"/>
                    <DataGridTextColumn Header="Message" Width="*" Binding="{Binding Message}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Window>
